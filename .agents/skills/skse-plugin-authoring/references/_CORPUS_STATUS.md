# skse-plugin-authoring — corpus status (dev-side; NOT shipped in the plugin)

*This file is the provenance ledger and staleness contract for the nine shipped references. It stays in
`skill-draft/` (and, after packaging, is the one `references/` file NOT bundled — same pattern as
`dialogue-authoring/references/_CORPUS_STATUS.md`). Readers of the shipped skill never see it; maintainers
refreshing the corpus start here.*

## What these references are

Hand-authored methodology, distilled from eight verified research mines over a **pinned** CommonLibSSE-NG
corpus (2026-07-03). They are NOT generated by construction — 99.7% of CommonLibSSE headers carry zero doc
comments, so there are no semantics to generate (the evaluation that chartered this project settled this).
The references are the *author-facing* transform of the dev-side research docs; the research docs remain the
full-detail source of record.

## Provenance — reference ← research doc ← corpus

Each shipped reference transforms one research doc under `../research/`. The research docs carry the full
claim inventory, adversarial verdicts, and `[BUILD-TEST]` markers; the references carry the distilled
guidance + verbatim idioms.

| Shipped reference | Research doc (`../research/`) | Mine |
|---|---|---|
| `toolchain-setup.md` | `toolchain-and-skeleton.md` | M1 |
| `plugin-skeleton.md` | `plugin-lifecycle-and-skse-api.md` (declare/lifecycle/interfaces) | M2 |
| `load-failures.md` | `plugin-lifecycle-and-skse-api.md` (refuse facet) | M2 |
| `event-sinks.md` | `event-sinks.md` | M3 |
| `native-papyrus-functions.md` | `native-papyrus-functions.md` | M4 |
| `hooking.md` | `hooking-and-relocation.md` | M5 |
| `multi-runtime.md` | `multi-runtime-discipline.md` | M6 |
| `threading-and-persistence.md` | `threading-and-persistence.md` | M7 |
| `plugin-dissection.md` | `plugin-dissection.md` | M8 |

## Corpus pins (the snapshot the references describe)

Every research doc header carries these; recorded once here as the refresh baseline.

- **PRIMARY — `alandtse/CommonLibVR` branch `ng` @ `8c048b3`** (2026-07-02; `commonlibsse-ng` 4.x lineage).
  The charter-locked current CommonLib. Also ships its own `CLAUDE.md` for AI agents (the multi-runtime
  patterns in `multi-runtime.md` are sourced from it).
- **CROSS-CHECK PROSE — CommonLibSSE-NG GitHub wiki @ `9b63d27`** (CharmedBaryon-era; the only prose docs,
  cross-checked against live `ng` code before anything was banked).
- **LOADER AUTHORITY — `ianpatt/skse64` @ `4c1b425`** (+ the `sksevr-2.0.12` clone for the VR loader gate).
  The refuse-to-load / lifecycle firing evidence in `load-failures.md` + `plugin-skeleton.md`.
- **EXEMPLARS — `spid` @ `bb0c227`, `po3-papyrus-extender` @ `c826bdf`, `oar` @ `eb19107`.** Production
  idioms. Each builds a specific CommonLib lineage — noted inline where an idiom is lineage-specific.
- **SCAFFOLD CANDIDATES — `colorglass-sample-plugin` @ `a3129b3`, `skyrimscripting-hello-world` @ `a547f5f`,
  `libxse-commonlibsse-template` @ `c882544`, `libxse-commonlibsse-ng-template` @ `d712dc1`,
  `epinter-skse-clibng-template` @ `b2cd936`, `skypal-ng`, `papyrusutil` @ `6212a5c` (raw SKSE64, contrast).**
- **CROSS-CHECK ONLY — `powerof3/CommonLibSSE dev` @ `a11cc72`** (no VR, no VariantID; different package).

## The [BUILD-TEST] debt — what an empirical pass (V0–V3) still owes

The mines marked every runtime-unprovable claim `[BUILD-TEST]`; the references reframed those as "Not yet
verified in-game" sections (all nine carry one). The debt is **read-from-source, not yet run**. The
load-bearing gates, by reference:

- **toolchain-setup** — the current-lineage *minimal CMake* scaffold is an unbuilt assembly (the xmake /
  submodule route is the proven one); ClibDT current state; libxse runtime coverage.
- **plugin-skeleton** — trampoline allocation is capacity-bounded, not time-gated (needs an AllocTrampoline-
  at-`kDataLoaded` observation); GOG/Epic store-variant version reconciliation; preload-phase interface legality.
- **load-failures** — the exact VR (`sksevr.log`) emitted-line shapes and the pre-AE SE loader gate;
  GOG/Epic address-library filename reconciliation.
- **event-sinks** — the raw-input-sink traversal (production-unattested; no exemplar sinks `InputEvent`);
  all runtime firing/threading/delivery claims; 16 opaque forward-declared event payloads.
- **native-papyrus-functions** — thread the VM invokes natives on; latent-function stack mechanics at runtime.
- **hooking** — SPID's `stl::install_hook` dispatch body (external submodule, unfetched); versionlib `.bin`
  unpack semantics; whether a swapped `RelocateVirtual` index reliably CTDs vs silently no-ops.
- **multi-runtime** — the CTD-vs-silent-misread distinction for several footguns.
- **threading-and-persistence** — off-thread-mutation corruption is doc-asserted, not observed; the exemplar
  CommonLib submodule SHAs (shallow clone can't verify objects).
- **plugin-dissection** — mostly method (low runtime-risk); the SPID mechanism map is read-confirmed.

Full residuals live in each research doc's "Residuals / open gaps" section. The V0–V3 validation ladder in
`../research/_research-roadmap.md` §3 is the plan to burn them; it is **owed to Aaron** and runs on his
machine/runtime (SE 1.6.1170). AE-other/VR claims stay doc-level until then.

## Field corroboration pass (2026-07-06)

A hardening pass folded in lessons from real, shipped CommonLib-NG production plugins (built, deployed, and
in several cases in-game-verified on a live SE/AE machine). These are **field observations**, distinct from
the V0–V3 empirical ladder (still owed), but several close or correct items above:

- **CORRECTED (was wrong):** `threading-and-persistence.md` previously advised "re-queue from inside the
  task for repeats." The task queue's drain is pop-until-empty *including tasks added during the drain*, so
  a self-requeued task busy-spins the main thread inside one frame — observed as a hard in-game freeze in
  two independent plugins. The reference now forbids self-requeue and teaches worker-thread pacing.
- **SOFTENED:** the CMake source-checkout route (`include(…/cmake/CommonLibSSE.cmake)` +
  `add_commonlibsse_plugin` + consumer manifest) is field-proven by multiple shipped plugins; the
  "unbuilt assembly" caveat now scopes only to the verbatim sketch. Practice notes added (manifest must
  satisfy CommonLib's `find_package` set incl. `rapidcsv`/`xbyak`; VS dev shell; cold-build time).
- **ADDED from production failures:** `write_call`/`write_branch` are instruction patches, not
  prologue-copying entry detours (hooking.md); funnel re-entrancy → freeze-then-stack-overflow-CTD pattern
  + guard discipline (hooking.md, event-sinks.md); lock-order inversion when calling a framework's API from
  its own callback while holding your own lock (threading); second-writer ping-pong / prefer published
  APIs / correct inputs not outputs (hooking.md, SKILL.md); event storms + coalescing (event-sinks.md);
  byte-exact `ReadRecordData` + bounds-before-allocate (threading); Debug-CRT `LoadLibrary` 126 failure
  (load-failures.md, toolchain); duplicate-metadata load failures (toolchain, SKILL.md); one-time-work
  idempotence via `once_flag` (plugin-skeleton.md); native-body discipline — never block/throw/log-per-call,
  no raw pointers past the call (native-papyrus-functions.md); async-only C++→Papyrus + VM-thread callbacks
  + `#undef GetObject` (native-papyrus-functions.md); ABI-faithful rebuild rules for patching installed
  plugins (toolchain-setup.md).

## Lineage watch (flag to Aaron)

The charter locks **alandtse `ng`** as current and nothing here challenges that. But the M1 mine surfaced,
with evidence, that **`libxse/commonlibsse` is an emerging fourth lineage** — libxse's flagship template
moved off NG onto it 2026-07-01 (the `REX::INFO` tell). Not a switch signal; a drift worth watching on the
next corpus refresh. The references teach recognizing it (so borrowed libxse code is read correctly), not
adopting it.

## Staleness duty

These references are hand-curated from a point-in-time snapshot of a **living** upstream (`ng` was pushed
the day before the pins). On any refresh: re-pin the corpus (record new commits here), re-run the affected
mine(s), and re-check the lineage watch above. The generated-by-construction guarantee that protects
`mutagen-reference` / `papyrus-reference` does **not** apply here — this corpus can silently drift, so the
re-check is a real duty, not a formality.

## Authoring-process notes

- **skill-creator was loaded** (`anthropic-skills:skill-creator`) at authoring time for its description-
  engineering guidance, per HOUSECARL_SKILL_AUTHORING.md §1. Its interactive eval loop (running
  claude-with-skill on test prompts + the review viewer) was **not** run — that is the empirical validation
  owed to Aaron, and it needs his review + machine. The project's own anonymized trigger fan-out (§6.5) is
  the shipping gate that WAS run — see `../skill-draft/evals/`.
- **The trigger fan-out** (recall ≥ 80%, specificity ≥ 50%) is the last gate before this leaves draft. The
  known risk it must clear: the "plugin"/ESP collision (the description disambiguates DLL vs `.esp` vs `.psc`).
- **Not yet done at draft stage** (checklist §8 items 16–19): eval set + fan-out (running next), and the
  `$Skills` registration in `scripts/build-plugin.ps1` (a packaging step, done when the skill lands).
